namespace TowerDefense.Enemies
{
    /// <summary>
    /// The concrete implementation for a standard enemy.
    /// Currently, it has no special behaviors and just inherits from BaseEnemy.
    /// </summary>
    public class StandardEnemy : BaseEnemy
    {
        // This enemy type has no special modifications, so we don't need to override anything.
        // It exists as a concrete class to be placed on a prefab.
    }
}